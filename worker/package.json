{
  "name": "shiftcommander-worker",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --local --port 8787",
    "start": "wrangler dev --local --port 8787",
    "smoke": "node scripts/smoke-routes.mjs",
    "check": "wrangler deploy --dry-run --outdir dist",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.18.0"
  }
}
