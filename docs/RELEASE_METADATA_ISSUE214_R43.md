# ShiftCommander R43: provider metadata access established

Assignment: Brian910cpr/910cpr-class-landers#214, dispatch `SHIFTCOMMANDER_ASTRA_20260913_R1`, continuing R42. Assessment: September 14, 2026, America/New_York (UTC-04:00). Release state: **BLOCKED**. System evidence: **BUILT**, with local synthetic validation and partial live connectivity; no complete operational PROVEN/MONITORED/HEALTHY claim.

The connected Cloudflare API successfully returned the previously missing Pages, Worker, routing and D1 metadata. **Do not continue treating metadata access as unavailable through every installed route.** R2's HTTP 401 was observed using another credential path; that path was neither repaired nor retried here. No production configuration changed.

## Current evidence and practical implications

Exact allowlisted observations are in `docs/PROVIDER_METADATA_ISSUE214_R43.json` (13 successful metadata reads, September 14 06:54:45-06:57:48 UTC) and `docs/PUBLIC_SERVING_ISSUE214_R43.json` (9 anonymous HTTP reads beginning 06:59:40 UTC, including referenced Pages JavaScript and CSS). No database rows, account credentials, binding values or member records were requested or retained.

| Surface | Verified evidence | Implication |
|---|---|---|
| Pages | Project `shiftcommander`, canonical production deployment `3e7c40e1-e950-4301-83fa-5c85a98b9db9`, created June 3, ad-hoc trigger on `codex/base44-worker-consolidation`, reported commit `9d949049bc7f5bf6acf937ef79ea90ef9987d0b9`. Source config is null; no environment-variable names returned. | The served frontend is an older consolidation deployment, not the draft Flask auth candidate. A configured production branch does not prove automatic GitHub deployment. |
| Frontend HTTP | Both `shiftcommander.pages.dev/` and `sc.adr-fr.org/` return 200. Pages JS `index-B0OhhaWu.js` and CSS `index-CsavDUu3.css` return 200. JS contains `https://shiftcommander-v2.onrender.com`; its hash matches R2. | Current default client-to-backend direction is consistent with the previously inspected Pages-to-Render path. Browser overrides and full authenticated navigation are not verified. |
| Custom frontend host | `sc.adr-fr.org` is a proxied CNAME to `shiftcommander.pages.dev`. Pages custom-domain list is empty, despite the successful public response. | Preserve the working host. Do not infer that an empty association list authorizes a DNS change. |
| Custom API host | `sc-api.adr-fr.org` is a proxied CNAME to tunnel `97f9d564-034a-44d6-9b1b-15836286d43a`; tunnel state is `down`, connection count 0, inactive since May 25, locally managed (`remote_config=false`). Anonymous `/api/health` returns 530. | This name is a failed tunnel path, not a custom domain on the deployed API Worker. The live frontend's default API host is Render, so repointing this name alone is not a demonstrated repair of the default customer workflow. |
| Worker | `shiftcommander-api`, deployment `5635f3af-07a2-4c4b-b9cd-cd3d649278b5`, version `e5319cf0-ab39-467e-8b9c-3ac89d68eac7` at 100%, created June 3. Workers.dev enabled; no associated Worker custom domains and no ADR zone Worker routes returned. | Distinct reachable Worker lane. Version metadata has no commit annotation; do not infer its exact source commit. |
| D1 | Worker settings and deployed-version bindings both map `DB` to `b8c79e7e-7513-453c-a73b-3cd6871bd146`; D1 metadata resolves that UUID to `adr_fr_scheduler`, 15 tables. | Remote database existence and this actual binding are established. Row correctness, bridge authorization, freshness and recovery are not established by metadata. |
| Render auth | Anonymous health returns `state_backend=d1`, `state_backend_ready=true`, `quick_test_mode=true`, `demo_supervisor_bypass=true`. Session returns `authenticated=false`, `auth_mode=quick_test`. | Development auth remains an observed release blocker. Component readiness does not prove trustworthy staffing or publication. |
| Worker auth | Anonymous session returns `authenticated=true`, `role=admin`, `local_worker_session=true`, `data_mode=local_json_seed`. | Stub authentication remains observed. No privileged write or member-data read was attempted. Do not route production clients here as an auth workaround. |

## Reproduction and limits

Use the existing connected Cloudflare API tool with GET only. Its account context supplies `{account_id}`; do not copy a bridge token into provider calls. The following endpoints produced the retained metadata:

- `/accounts/{account_id}/pages/projects/shiftcommander` and `/domains` beneath that project.
- `/accounts/{account_id}/workers/scripts/shiftcommander-api/settings`, `/deployments`, `/subdomain`, and `/versions/e5319cf0-ab39-467e-8b9c-3ac89d68eac7`.
- `/accounts/{account_id}/workers/domains?service=shiftcommander-api`.
- `/zones?name=adr-fr.org&account.id={account_id}`; ADR zone `434689a6702318ff3cc4959fad1d1286` `/workers/routes` and `/dns_records?name=<sc.adr-fr.org or sc-api.adr-fr.org>&type=CNAME`.
- `/accounts/{account_id}/d1/database/b8c79e7e-7513-453c-a73b-3cd6871bd146`.
- `/accounts/{account_id}/cfd_tunnel/97f9d564-034a-44d6-9b1b-15836286d43a`.

Return allowlisted metadata only: names/IDs, routing destinations, timestamps, deployment status, binding names/types and D1 IDs. Omit environment/binding values, credentials, account contact fields and raw source. Binding name `SC_D1_BRIDGE_TOKEN` is not its value. Public probes use anonymous GET with TLS validation and retain hashes plus selected status/auth-mode fields, never response payloads or operational schedule rows. The JSON records exact URLs, timestamps and hashes. These probes are HTTP/source evidence, not a visual browser test.

One initial Pages list call (`page=1, per_page=100`) returned error 8000024, invalid list options. A direct GET of the known project succeeded; no authorization failure was retried. The initial public probe had an unnecessary second asset fetch that returned 403 and aborted before its output was persisted. The corrected probe fetches/parses each response once and records HTTP errors. Its final run completed: eight 200 responses and one 530. This establishes the final observations, not continuous availability.

## Remaining gates and concrete next work

1. **Private persistent auth:** approve the filesystem and exact `SC_AUTH_DB_PATH`, schema version 2 readiness, privately provisioned real members/named supervisors, signing configuration and inherited hosting settings. No approved configuration was supplied through #214. Preserve the opt-in candidate until coordinated staging; do not disable the current service, activate against schema v1, or restore old revoked sessions.
2. **Current ADR inputs:** approve current roster/certifications, per-unit `qualOp`, availability consent, demand and calendar provenance. The 170-shift schedule ending August 10 is historical R2 evidence, not a fresh R43 schedule read. ADR Google Calendar remains published-staffing authority; Blank remains do-not-auto-schedule.
3. **Credential incident:** R37 reported a bridge credential in local tool output. Private containment/coordinated rotation and old-credential rejection proof remain unverified. This worker did not retrieve, reuse, disclose or rotate that credential. Never place it in an issue or receipt.
4. **Coordinated staging:** retain draft Flask PR #10 and its stack. Review its authenticated member/supervisor boundary against the now verified Pages-to-Render client path, scoped bootstrap and session-expiry behavior; independently secure or exclude the Worker stub and obsolete tunnel path. Restoring/repointing the tunnel or altering frontend API authority is a production cutover, not an automatic consequence of this metadata read. Review the concrete routing proposal once the auth configuration is prepared.
5. **Release proof:** real availability -> durable readback/restart -> legal resolver -> supervisor review -> publication, with matching member/mobile/wallboard revision; hosted backup/recovery and observer heartbeat. Retain partial/overnight/DST, Blank, ALS/driver shortages, locks, OT, swaps, duplicate and unauthorized submissions, Windows application start/stop, and phone/SMS/email intake in scope.

The changed evidence makes serving metadata review actionable now. Remaining account/input approvals still prevent release; this report does not authorize merge, deployment, auth activation, data migration or staffing-policy changes. Next ChatGPT action: review these exact observations, retire the blanket Cloudflare metadata-access blocker, obtain the two private configuration/current-input approvals and credential-incident disposition, then dispatch the coordinated staging tranche against these verified serving paths. Do not send another unchanged access-restoration-only round.

## Validation, preservation and handoff

Target worktree `E:\GitHub\shiftcommander_v2_codex_issue214_r43`, branch `codex/issue-214-provider-metadata-r43`, based on R9 `16d0ace259b485a7585decbef24c74e94bd69f5c`. Application remains R8 `ba0365a250d18297a262b96ab7f15cf3fe6f1780`, OPEN draft [PR #10](https://github.com/Brian910cpr/shiftcommander_v2/pull/10), unchanged three-file scope and no CI results. Fetched main remains `67a3f88f1b54fa2ffbd285df7df969cea7837616`; no fresh Render provider deployment read is claimed.

Fresh local AST/in-memory compilation passed for `server.py`, `engine/auth_store.py`, `engine/live_state_store.py` in the read-only R9 worktree. The R2 `scripts/Start-AstraReview.ps1` passed PowerShell parsing. Retained `debug/verification_r9/combined_final.log` records `Ran 160 tests in 189.801s`, `OK`, `FINAL: tests=160 failures=0 errors=0 skips=0`; those unchanged behavioral suites were not rerun. They establish synthetic local auth/restart/audit/recovery/resolver evidence only. JSON parsing, metadata relationships, timestamp/count/status checks, Markdown/whitespace and explicit-file Git scope are checked before commit. No generator ran.

Only three report/evidence files change here: `docs/RELEASE_METADATA_ISSUE214_R43.md`, `docs/PROVIDER_METADATA_ISSUE214_R43.json`, `docs/PUBLIC_SERVING_ISSUE214_R43.json`. They are pushed for review; the courier receipt supplies the exact target commit. No application PR, code/config/test change, merge, deployment or production write is made by this checkpoint. Target sparse initialization left 48 intended root/docs paths unpopulated; the empty directory, expected path scope and empty staged diff were inspected before non-forced index population. Original dirty checkouts, four unpublished consolidation commits and earlier worktrees remain preserved.

Local active-thread `turn_context` reports `gpt-6-astra` at `2026-09-14T06:50:55.472Z`, matching session CLI `0.153.4`. This is runtime-local evidence, not provider attestation. Existing project launcher CheckOnly at `2026-09-14T02:53:45.1329755-04:00` reports dispatcher lock held/inaccessible, `can_launch=false`. No duplicate launch, lease/lock/default change or new timer. The Astra launcher is not an application start command; no normal operational localhost URL is verified.

Proof contract: success must tie one real member availability save, persisted revision, legal explained publication and all rendered views together. No complete real-world last-success timestamp exists in reviewed evidence. Wednesday 23:59 publication/freshness must be exercised with approved inputs. Lost saves, inaccessible storage, stale sources, illegal assignments, mismatched views and missed publication are failures. Whole-workflow observer, observer heartbeat and recovery/escalation delivery remain unproven. Local tests run on change and are not that observer. Preserve failed evidence and recover approved credentials without resurrecting revoked sessions, following `docs/RELEASE_CHECKLIST_ISSUE214_R6.md`; reconcile staffing history before cutover.

Exact prior review sources remain `docs/RELEASE_VERIFICATION_ISSUE214_R9.md`, `docs/RELEASE_CHECKLIST_ISSUE214_R8.md`, R6 recovery/schema guidance, and commit `286876e7d506bd127e14c2852f65c827815a8fa7:docs/RELEASE_EVIDENCE_ISSUE214_R2.json` (`read_only_checks`). Application review sources remain `server.py`, `engine/auth_store.py`, `engine/live_state_store.py`, and the eight test paths enumerated by R9. Remote work was limited to GitHub, connected Cloudflare metadata and anonymous public HTTP; processing and validation were local.
